jupyter
pandas
lxml
bson
tqdm
urllib3
pytools

# higher versions require Python 3.7
numpy==1.19.0

beautifulsoup4

ir_datasets

pyjnius

torch
torchtext
# Let's not install fasttext by default
# 0.9.2 fails to install in some cases
#fasttext==0.9.1

# transformers can't tolerate tokenizer version that it's too high
tokenizers<0.11
# let's fix transformers versions, this is the key library and 
# each upgrade can be a surprise
transformers==4.15.0
# same applies to spacy
spacy==2.2.3
sentence-transformers
sentencepiece

thrift==0.13.0
typing-extensions
